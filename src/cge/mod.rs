//! EANT2 represents neural networks and their parameters in a compact way, the Compact Genetic Encoding aka the "linear genome".
//! It encodes the topology of the network implicitly by the order of its elements (genes).

pub mod node;
